# Tabs

The Jobs tab is permanent and non-closable. Job URLs are canonical tab identities. Opening an existing URL selects its tab. Closing an active job tab selects its right neighbor, then its left neighbor, and finally the Jobs tab.
